%h1 Editing demo
- form_tag :action => 'update', :id => @demo do
  = render :partial => 'form'
  = submit_tag 'Edit'
= link_to 'Show', :action => 'show', :id => @demo
|
\#{link_to 'Back', :action => 'list'}
