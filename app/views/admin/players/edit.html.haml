%h1 Editing player
= form_tag :action => 'update', :id => @player do
  = render :partial => 'form'
  = submit_tag 'Edit'
= link_to 'Show', :action => 'show', :id => @player
|
\#{link_to 'Back', :action => 'list'}
