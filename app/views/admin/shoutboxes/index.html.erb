<h1>Listing shoutboxes</h1>

<table>
  <tr>
    <th>Created at</th>
    <th>Name</th>
    <th>Message</th>
  </tr>

<% for shoutbox in @shoutboxes %>
  <tr>
    <td><%= render_datetime shoutbox.created_at %></td>
    <td><%= render_nickname shoutbox.name %></td>
    <td><%= h shoutbox.message %></td>
    <td><%= link_to 'Show', admin_comment_path(shoutbox) %></td>
    <td><%= link_to 'Edit', edit_admin_comment_path(shoutbox) %></td>
    <td class="destroy"><%= link_to 'Destroy', admin_comment_path(shoutbox) %></td>
  </tr>
<% end %>
</table>

<div style="text-align: center; padding-top: 1em">
  <%= will_paginate @shoutboxes, :inner_window => 2, :outer_window => 0 %>
</div>

