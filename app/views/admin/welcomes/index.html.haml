%h1 Welcome.

%h2 Demos having trouble
- if @demos.length == 0
  %p Everything looks fine. =)
- else
  %table
    %tr
      %th id
      %th created
      %th gamemode
    - for demo in @demos
      %tr
        %td= demo.id
        %td= render_datetime demo.created_at
        %td= h demo.gamemode
        %td{:style => "color: red"}= (demo.data_correct.nil?) ? 'NOT VERIFIED' : 'DATA INCORRECT'
        %td= link_to 'Verify', verify_demo_path(demo)
        %td= link_to 'Edit', edit_admin_demo_path(demo)
        %td= link_to 'Destroy', admin_demo_path(demo), :confirm => 'Are you sure?', :method => :delete

