%h1 Comments

%table
  %tr
    %th By
    %th Comment
    %th On
  - for comment in @comments
    %tr
      %td.nowrap
        = gravatar_tag comment, 20
        = render_nickname comment.name
      %td
        = comment.message
      %td.nowrap
        = render_linked_players(comment.demo.players)
        on
        = render_linked_map comment.demo.map
      %td.nowrap
        = link_to 'Play', :controller => 'demos', :action => 'show', :id => comment.demo

%div{:style => "text-align: center; padding-top: 1em"}
  = will_paginate @comments

